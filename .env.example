# Base URL for the MarketScout API. Leave empty to use the Vite dev-server proxy
# (vite.config.ts forwards /v1 -> http://localhost:8100).
VITE_API_BASE_URL=

# Only needed if the backend has AUTH_DISABLED=false — sent as the X-API-Key header.
VITE_API_KEY=
